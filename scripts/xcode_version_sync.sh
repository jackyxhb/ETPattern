#!/bin/bash

# TEMPORARY: Script disabled to debug build failure.
# This ensures the script phase passes successfully without doing anything.

echo "⚠️ Version sync disabled temporarily for debugging."
exit 0
